{
  "name": "rag_system",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build && cd ../frontend && npm install && npm run build",
    "start": "npm start --prefix backend"
  }
}
